enclave {
    trusted {
        /* define ECALLs here. */
		public void t_request([in, string] char *request);
    };

    untrusted {
        /* define OCALLs here. */
		void u_dump_secret([in, string] char *secret);
    };
};
